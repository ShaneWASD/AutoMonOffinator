# AutoMonOffinator
AutoMonOffinator is an OLED care tool that prevents burn-in by dimming static UI elements. It includes dimmers for taskbar, browser UI, screen (idle &amp; focus dimming), and custom apps via profiles. Features tray icon, watchdog, and multilingual config console.
